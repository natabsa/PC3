//import {} from "../confid/"dbConfig


// Funcao que retorna o registro do objeto com o id `id` que e do tipo `obj`
export async function read(obj, id)
{
    
}

// Fucao que cria um registro do objeto com os dados `body` que sera do tipo `obj`
export async function create(obj, body)
{
    
}

// Funcao que atualiza o registro do objeto com o id `id` que e do tipo dados `body`
export async function update(obj, id, body)
{
    
}
// Funcao que deleta o registro do objeto com o id `id` que e do tipo `obj`
export async function del(obj, id)
{
    
}

// Funcao que envia, por email, a senha do usuario com o id `id`
export async function enviarSenha(id)
{
    
}
